services:
  app:
    image: ghcr.io/piecethelord/naskatest-app:latest
    container_name: naskatest-app-api
    restart: always
    volumes:
      - /etc/letsencrypt/live/alexey-web-dev.ru/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - /etc/letsencrypt/live/alexey-web-dev.ru/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.naska.rule=Host(`naska.alexey-web-dev.ru`)"
      - "traefik.http.routers.naska.entrypoints=websecure"
      - "traefik.http.routers.naska.tls.certresolver=myresolver"
      - "traefik.http.services.naska.loadbalancer.server.port=5000"

networks:
  web:
    external: true